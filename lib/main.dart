import 'package:flutter/material.dart';

void main() {
  runApp(NetStratix());
}

class NetStratix extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return MaterialApp(
      title: 'Net Stratix Technologies',
      home: Home()
    );
  }
}
